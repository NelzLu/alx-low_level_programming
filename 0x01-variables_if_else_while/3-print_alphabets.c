#include <stdio.h>

/**
 * main - print the alphabets in lowercase and then in uppercase
 * Return: Always 0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++
		purchar (ch);

	for (ch = 'A'; ch <= 'Z'; ch++
		purchar (ch);

	putchar('\n');

	return (0);
}
